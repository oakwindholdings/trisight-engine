The componentHierarchy.png could not be generated via ImageMagick quoting on Windows shell.
Use the Mermaid source at componentHierarchy.mmd. You can render it with the Mermaid CLI:

mmdc -i docs/admin-prep/componentHierarchy.mmd -o docs/admin-prep/componentHierarchy.png

